@model CorporationHR.Models.SecurityOfTable

@{
    ViewBag.Title = "Edit";
}

<h2>Edit table</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SecurityOfTable</legend>

        @Html.HiddenFor(model => model.TableId)

        <div class="editor-label">
            @Html.LabelFor(model => model.TableName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TableName)
            @Html.ValidationMessageFor(model => model.TableName)
        </div>
        <div class="display-label">
            @Html.DisplayNameFor(model => model.SelectedClearenceId)
        </div>
        <div class="display-field">
            @Html.DropDownListFor(model => model.SelectedClearenceId, Model.Clearences)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
